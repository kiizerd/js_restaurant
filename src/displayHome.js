const Display = (function () {
  


  function getFeatured() {
    const div = document.createElement('div');
    div.classList.add('card-group');

    const firstItem = getFeateuredItem(1)
    const secondItem = getFeateuredItem(2)
    const thirdItem = getFeateuredItem(3)
  };

  function getFeaturedItem(itemNum) {
    const div = document.createElement('div');
  }

  return {}
})();